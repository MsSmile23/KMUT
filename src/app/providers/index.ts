import { AccessProvider } from './AccessProvider';
import { InterfaceProvider } from './InterfaceProvider';
import { IntitalLoadProvider } from './IntitalLoadProvider';
import { LicenseProvider } from './LicenseProvider';
import { PreloadProvider } from './PreloadProvider';
import { PrevLocationProvider } from './PrevLocationProvider';

export {
    AccessProvider,
    PreloadProvider,
    InterfaceProvider,
    IntitalLoadProvider,
    PrevLocationProvider,
    LicenseProvider
}